# MCP Server

Custom Model Context Protocol server for extending the kernel tool layer.

## Skills

- `generate-api`

## Register with kernel

Add your MCP server in Settings, then include it in `buselligence.lock` for reproducibility.

```json
{
  "mcpServers": ["my-custom-server"]
}
```
