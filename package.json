{
  "name": "buselligence",
  "version": "8.0.0",
  "description": "The Buselligence Project — open-source AI runtime by Salestrics Inc. (MIT). Buselligence™ is a trademark of Salestrics Inc.",
  "license": "MIT",
  "author": "Salestrics Inc.",
  "homepage": "https://github.com/Salestrics/Buselligence",
  "repository": {
    "type": "git",
    "url": "https://github.com/Salestrics/Buselligence.git"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server && npm run build --prefix cli",
    "start": "npm run start --prefix server",
    "setup": "npm install --prefix client && npm install --prefix server && npm install --prefix cli && npm run db:migrate --prefix server && npm run db:seed --prefix server",
    "postsetup": "node scripts/welcome.js",
    "bus": "node cli/dist/index.js",
    "cli:build": "npm run build --prefix cli"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
